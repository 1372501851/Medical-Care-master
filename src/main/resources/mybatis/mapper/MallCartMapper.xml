<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.merchant.mapper.MallCartMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.project.merchant.domain.MallCart">
            <id property="id" column="id" jdbcType="VARCHAR"/>
            <result property="productId" column="product_id" jdbcType="VARCHAR"/>
            <result property="userId" column="user_id" jdbcType="VARCHAR"/>
            <result property="selected" column="selected" jdbcType="CHAR"/>
            <result property="quantity" column="quantity" />
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,product_id,user_id,
        selected,quantity,create_time,
        update_time
    </sql>
    <insert id="insertSelective" parameterType="MallCart">
        insert into mall_cart
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userId != null">user_id,</if>
            <if test="productId != null">product_id,</if>
            <if test="selected != null">selected,</if>
            <if test="quantity != null">quantity,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="userId != null">#{userId},</if>
            <if test="productId != null">#{productId},</if>
            <if test="selected != null">#{selected},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective">
        update mall_cart
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="productId != null">product_id = #{productId},</if>
            <if test="selected != null">selected = #{selected},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>
    <update id="updateSelectedOne">
        update mall_cart
        set selected = #{selected}
        where product_id = #{productId} and user_id = #{userId}
    </update>
    <update id="updateSelectedAll">
        update mall_cart
        set selected = #{selected}
        where  user_id = #{userId}
    </update>
    <delete id="deleteByPrimaryKey">
        delete from mall_cart where id = #{id}
    </delete>
    <select id="selectCartByUserIdAndProductId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from mall_cart
        where product_id = #{productId} and user_id = #{userId}
    </select>
    <select id="selectList" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from mall_cart
        where user_id = #{userId}
    </select>
</mapper>
